(()=>{const read=key=>{try{return JSON.parse(localStorage.getItem(key))||[]}catch{return[]}};const favorites=read('monsaba-favorites-v1'),recent=read('monsaba-recent-tata-v1');const render=(id,items)=>{document.querySelector(id).innerHTML=items.map(item=>`<a href="${item.href}">${item.name}</a>`).join('')};render('#favoriteList',favorites);render('#recentList',recent);document.querySelector('#personalEmpty').hidden=Boolean(favorites.length||recent.length)})();
